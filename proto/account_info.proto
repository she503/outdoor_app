syntax = "proto2";

package proto;

enum PermissionLevel {
    UNKNOWN = 0;
    NORMAL = 1;
    ADMIN = 2;
};

message Account {
    optional PermissionLevel level = 1;
    optional string user_name = 2;
    optional string password = 3;
}

message AccountInfo {
    repeated Account account = 1;
}